class FinalFrame {
	constructor(roll1, roll2, roll3) {
	    this.roll1 = roll1;
	    this.roll2 = roll2;
	    
		if (roll1 + roll2 >= 10 ) { //strike or spare
			this.roll3 = roll3
		}
		else {	
			this.roll3 = 0;
		}

		this.total = roll1 + roll2 + this.roll3;
  	}
}

module.exports = FinalFrame;